import 'package:flutter/material.dart';

AlertDialog escolherNomesJogadores(){
  return AlertDialog(
    content: Column(
      children: [
        TextField(
          
        )
      ],
    ),
  );
}