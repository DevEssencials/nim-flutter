
import 'package:flutter/material.dart';

Container formaContainer(){
  return Container(
    alignment: Alignment.center,
    decoration: BoxDecoration(
      color: Colors.pink,
      borderRadius: BorderRadius.circular(30),
    ),
  );
}